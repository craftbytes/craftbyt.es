---
export interface Props {
  class?: string;
}

const { class: className } = Astro.props;
const cls = className || '';
---

<svg viewBox="0 0 130 32" aria-hidden="true" class:list={[cls]}>
  <image href="/cb-logo-type.svg" width="130" height="32" />
</svg>
